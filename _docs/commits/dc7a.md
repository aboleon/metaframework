# Namespaced package translations

Moves MetaFramework's standard language files into its runtime translation resources, migrates package consumers to the `mfw::` namespace, and publishes optional overrides to `lang/vendor/mfw`.

Adds forward migration confirmation translations and verifies namespace loading, sparse application overrides, and publication paths.
